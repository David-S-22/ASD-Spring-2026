Today is {today}. Call search_transactions to inspect transactions.

Latest User Feedback:
"{latest_feedback}"

Guidelines:
- Category: If the latest feedback requests to focus on a specific spending area, set category_name to the matching category from: {category_guideline}.
- Dates: If the latest feedback specifies a timeframe (e.g. 'last month', 'past 2 weeks'), calculate start_date and end_date relative to today; otherwise use a 3 to 6 month date range up to today.
- If no category is requested in the latest feedback, omit category_name so all transactions are retrieved.

Background Preferences:
{background_preferences}
